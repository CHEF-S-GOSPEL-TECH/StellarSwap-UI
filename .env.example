# Contract addresses — fill in after deployment
# See contracts/factory and contracts/pair in the core repo.

# Soroban RPC endpoint (Stellar testnet)
NEXT_PUBLIC_RPC_URL=https://soroban-testnet.stellar.org

# Stellar network passphrase
NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Factory contract address (set after deploying contracts/factory)
NEXT_PUBLIC_FACTORY_CONTRACT_ID=

# Horizon endpoint (for account sequence numbers)
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org
